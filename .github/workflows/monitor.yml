name: Medicaid Fee Schedule Monitor
on:
  schedule:
    - cron: '0 12 * * 1-5'   # Mon–Fri at 12:00 UTC (8am ET)
  workflow_dispatch:
permissions:
  contents: write
jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Run monitor
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          EMAIL_TO:  ${{ secrets.EMAIL_TO }}
        run: python main.py
      - name: Commit updated data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f data/checksums.json 2>/dev/null || true
          git add -f data/base_rates.json 2>/dev/null || true
          git add -f data/rate_comparison.json 2>/dev/null || true
          git add -f data/report.html 2>/dev/null || true
          git add -f data/mco/ 2>/dev/null || true
          git diff --cached --quiet && echo "No changes to commit" || (git commit -m "chore: update data $(date -u +'%Y-%m-%d')" && git push)
