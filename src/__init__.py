# package
```

**If `__init__.py` doesn't exist in the src folder at all**, click **Add file → Create new file**, type `src/__init__.py`, paste `# package`, and commit.

**Then — verify your src folder looks exactly like this** by clicking into it on GitHub:
```
src/
├── __init__.py
├── scraper.py
├── parser.py
├── exposure.py
└── emailer.py
